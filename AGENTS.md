# Codex project guidance

이 지침은 저장소 전체에 적용된다.

## 기준 정보

- 기준 저장소는 `RiaRon/robot_control-jazzy`이다.
- `RiaRon/robot_control`은 선배의 `divingyoon/robot_control`을 fork한 별도
  저장소이다. 사용자가 그 정확한 작업을 명시적으로 요청하지 않는 한 선배
  저장소에 push하거나 Pull Request를 만들거나 현재 저장소의 remote를 그쪽으로
  변경하지 않는다.
- push하기 전에 `git remote -v`로 대상이
  `https://github.com/RiaRon/robot_control-jazzy.git`인지 확인한다.
- 배포 또는 실물 테스트 작업 전에는 `README.md`와
  `docs/openarm-test-workflow.md`를 읽고 해당 절차를 따른다.

## 선배 GitHub에서 필요한 코드 가져오기

- 가짜 실험, 빌드, OpenArm 실행에 필요한 코드나 자산이 로컬에 없으면 선배의
  공개 GitHub 저장소를 읽기 전용 원본으로 검색하고 필요한 내용을 가져와도 된다.
  현재 알려진 원본은 `divingyoon/robot_control`과 `divingyoon/hdgp`이다.
- 선배 저장소는 fetch·clone·비교만 한다. 로컬 clone의 push URL은 가능하면
  `DISABLED`로 설정하고, 사용자가 그 정확한 외부 작업을 명시적으로 요청하지
  않는 한 선배 저장소에 push하거나 Pull Request를 만들지 않는다.
- 가져오기 전에 원본 저장소, 브랜치, 커밋, 라이선스를 확인한다. 라이선스가
  불명확하거나 현재 저장소와 호환되지 않으면 코드를 복사하지 말고 사용자에게
  알린다.
- 외부 코드나 자산을 무조건 복사하지 않는다. 형제 저장소 의존성으로 유지해야
  하는지, 현재 저장소에 최소 변경으로 포함해야 하는지 먼저 판단한다.
- 현재 프로젝트에 변경이 필요하면 최신 `jazzy`에서 `feature/` 브랜치를 만들고,
  Jazzy 구조와 안전 규칙에 맞게 필요한 부분만 수정한 뒤 테스트한다.
- 검증된 변경은 `RiaRon/robot_control-jazzy`에만 push하고 Pull Request로
  `jazzy`에 반영한다.
- 완료 보고에는 원본 저장소·브랜치·커밋, 가져온 내용, 실제 수정 파일, 수정 이유,
  테스트 결과, 본인 저장소의 브랜치·커밋·Pull Request를 빠짐없이 적는다.

## 브랜치

- `jazzy`는 Ubuntu 24.04와 ROS 2 Jazzy용 기본·안정 브랜치이다.
- `humble`은 ROS 2 Humble용 별도 장기 브랜치이다. 두 배포판 브랜치를 통째로
  merge하지 않는다.
- 일반 코드 수정은 최신 `jazzy`에서 `feature/짧은-이름` 브랜치를 만들어
  진행한다.
- OpenArm 컴퓨터에서 돌아온 작은 테스트 변경은 `test/날짜-짧은-이름`
  브랜치에서 검토한다.
- 사용자가 정확한 대상과 의도를 명시하지 않는 한 force push나 원격 브랜치
  삭제를 하지 않는다.
- 기존 작업 폴더의 사용자 변경과 관계없는 브랜치를 보존한다.

## 컴퓨터와 데이터의 역할

- 이 개발 PC의 `/home/cbj4/robot_control-jazzy`를 코드 수정의 기준 작업 폴더로
  사용한다.
- OpenArm 컴퓨터는 빌드, 배포, 실물 실행과 측정을 위한 테스트 환경으로 다룬다.
  테스트 중 필요한 최소 변경만 별도 `test/` 브랜치에 남긴다.
- 작은 설정, 테스트 코드와 결과 요약은 GitHub로 전달할 수 있다.
- rosbag, HDF5, MCAP, 영상, 대용량 센서 원본과 생성물은 Git에 넣지 않고 USB
  또는 별도 실험 데이터 저장소로 전달한다.
- `ros_ws/build/`, `ros_ws/install/`, `ros_ws/log/`, `artifacts/`, `bags/`와
  `.gitignore` 대상 파일을 커밋하지 않는다.
- 토큰, 비밀번호, 장비 비밀값을 파일, 커밋, 로그에 남기지 않는다.

## 작업 절차

1. 작업 전 `git status --short --branch`, `git remote -v`, 현재 기준 커밋을
   확인한다.
2. 변경 범위를 작게 유지하고 관련 없는 사용자 변경을 수정하지 않는다.
3. 코드 변경은 위험에 비례해 로컬 테스트와 정적 검사를 수행한다.
4. 필요한 코드가 없으면 위의 읽기 전용 원본 절차로 선배 GitHub를 확인하고,
   출처와 버전을 기록한 뒤 현재 프로젝트에 맞게 최소 범위로 반영한다.
5. OpenArm 테스트 결과를 가져올 때 먼저 기능 브랜치와 테스트 브랜치의 diff를
   검토한다.
6. 검증된 변경만 선형 이력을 유지하며 `jazzy`에 반영한다.
7. 완료 보고에는 브랜치, 최종 커밋, 실행한 검사와 수행하지 못한 실물 검사를
   구분해서 적는다.

## 실물 하드웨어 안전

- 기본값은 가짜 하드웨어와 드라이런이다.
- 사용자가 현재 작업에서 실물 동작을 명시적으로 요청하지 않았다면 로봇을
  움직일 수 있는 명령을 실행하지 않는다.
- `--real`, `--execute`, CAN 인터페이스 이름, 좌우 팔 매핑을 추측하지 않는다.
- 실물 실행 전 현재 브랜치와 커밋, CAN FD 링크 상태, 좌우 CAN 매핑을 확인한다.
- README에 설명된 `CommandGate`, 위치·속도 제한, 워치독과 명시적 실행 승인을
  우회하지 않는다.
- 실물 결과가 필요한 검증은 로컬 시뮬레이션 성공만으로 통과했다고 보고하지
  않는다.

## 사용자와의 소통

- 사용자가 Git이나 ROS 2에 익숙하지 않을 수 있으므로 결과와 영향을 한국어로
  간단히 설명한다.
- 삭제, force push, 선배 저장소 대상 작업, 실물 구동처럼 되돌리기 어렵거나
  외부에 영향을 주는 작업은 대상을 명확히 확인한다.
